/*write a program to calculate the area of square
FORMULE = side*side */
#include<stdio.h>
int main(){

float side;
printf("Enter the side ");
scanf("%f",&side);
printf("Area is %f",side*side);
return 0;
}
